<?php

defined('MOODLE_INTERNAL') || die();

// don't forget to bump this in case of change in local/db ...
$plugin->version   = 2012012413;        // The current plugin version (Date: YYYYMMDDXX)

$plugin->requires  = 2011112900;        // Requires this Moodle version (Moodle 2.0 ) 
$plugin->component = 'local_insalyon';       // Full name of the plugin (used for diagnostics)

?>
